{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "outputMode": "new-only"
    },
    "dev": {
      "dependsOn": [],
      "outputMode": "new-only"
    },
    "pack": {
      "dependsOn": [],
      "outputMode": "new-only"
    },
    "test": {
      "dependsOn": ["build"],
      "outputMode": "new-only"
    },
    "test:integration": {
      "dependsOn": ["build"],
      "outputMode": "new-only"
    },
    "test:ui": {
      "dependsOn": ["build"],
      "outputMode": "new-only"
    },
    "start": {
      "dependsOn": [],
      "outputMode": "new-only"
    },
    "version": {
      "dependsOn": [],
      "outputMode": "new-only"
    }
  }
}
