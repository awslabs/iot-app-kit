import React from 'react';
import { ExpandableSection } from '@cloudscape-design/components';

const ChartSettings = () => {
  return (
    <ExpandableSection headerText="Chart settings" defaultExpanded>
      --
    </ExpandableSection>
  );
};

export default ChartSettings;
