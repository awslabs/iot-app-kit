import React from 'react';
import { ExpandableSection } from '@cloudscape-design/components';

const DataSettings = () => {
  return (
    <ExpandableSection headerText="Data settings" defaultExpanded>
      --
    </ExpandableSection>
  );
};

export default DataSettings;
