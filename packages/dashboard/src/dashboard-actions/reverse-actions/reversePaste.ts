import { DeleteAction, PasteAction } from '../../types';
/*
export const reversePaste = (pasteAction: PasteAction): DeleteAction => {
  const newPasteAction: PasteAction = pasteAction;
  newResizeAction.payload.changeInPosition.x = pasteAction.payload.copyGroup;
  newResizeAction.payload.changeInPosition.y = resizeAction.payload.changeInPosition.y * -1;
  return newResizeAction;
};
*/
