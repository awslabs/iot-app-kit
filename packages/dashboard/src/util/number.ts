/**
 * Returns the non-negative value of a given number.
 * @param number - The input number.
 * @returns The non-negative value of the input number.
 */
export const nonNegative = (number: number) => Math.max(0, number);
