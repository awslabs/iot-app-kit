import type { Integer } from '@iot-app-kit/helpers/number/integer';

/** Size of a cell in pixels. */
export type CellSize = Integer;

/** Index of a cell along an axis. */
export type CellIndex = number;
