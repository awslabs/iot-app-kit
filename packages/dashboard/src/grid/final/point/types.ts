/**
 * Point in two-dimensional space.
 */
export interface Point {
  /** Cell position of the point along the x-axis. */
  readonly x: number;
  /** Cell position of the point along the y-axis. */
  readonly y: number;
}
