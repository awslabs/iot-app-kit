import { Messages } from '../types';

export type TableMessageKeys = 'table-header.propertySelectionLimit';

export const Table: Messages<TableMessageKeys> = {
  en: {
    'table-header.propertySelectionLimit':
      'For optimal AI summary results, please select up to {limit} properties from the table below.',
  },
};
